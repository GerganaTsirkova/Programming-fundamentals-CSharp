using System;
using System.Collections.Generic;
using System.Linq;

namespace LIS
{
    class Program
    {
        static void Main(string[] args)
        {
            List<int> len = Console.ReadLine().Split(new char[] { ' ' }, StringSplitOptions.RemoveEmptyEntries).Select(int.Parse).ToList();
            List<int> resultList = new List<int>();
            int counter = 0;
            
            for (int i = 0; i < len.Count-1; i++)
            {
                
                
            }
        }
    }
}
